
import { Post, User } from './types';

// Semua data dummy dihapus. Aplikasi sekarang murni menggunakan Supabase.
export const CURRENT_USER: User | null = null;
export const MOCK_USERS: User[] = [];
export const INITIAL_POSTS: Post[] = [];
